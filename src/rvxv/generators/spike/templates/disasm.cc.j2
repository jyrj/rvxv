// Auto-generated by RVXV — RISC-V AI Extension Verification Platform
// Disassembly support for custom instructions.
// Do not edit manually. Regenerate with: rvxv generate --spec <spec.yaml>
//
// NOTE: Disassembly is registered via the extension_t::get_disasms() override
// in extension.cc. This file provides a standalone register_custom_disasm()
// function for use cases where the extension is integrated manually.

#include "disasm.h"
#include "rvxv_encoding.h"

void register_custom_disasm(disassembler_t* d) {
{% for entry in entries %}
  // Register {{ entry.name }}: {{ entry.description }}
  d->add_insn(new disasm_insn_t(
    "{{ entry.name }}",
    MATCH_{{ entry.name_upper }},
    MASK_{{ entry.name_upper }},
    { {% for arg in entry.disasm_args %}new {{ arg }}{% if not loop.last %}, {% endif %}{% endfor %} }
  ));
{% endfor %}
}
