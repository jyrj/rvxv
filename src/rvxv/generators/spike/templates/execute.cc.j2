// Auto-generated by RVXV: {{ spec.name }} — {{ spec.description }}
require_vector_vs;
{% if is_widening %}
// Widening operation: SEW matches source element width ({{ src_sew_bits }}b),
// destination is wider ({{ dest.width_bits }}b).
{% endif %}
{% if not is_reduction %}
require(P.VU.vsew == {{ sew_enum(sew_bits) }});
{% endif %}

{% include body_template %}
